#Tuples are a limited version of list; they are sort of a more efficient version of lists that you cannot modify;
#they have elements which are indexed starting at 0

x = ('Glenn', 'Sally', 'Joseph')
print(x[2])
y = (1,9,2)
print(y)
print(max(y))
for iter in y:
	print(iter)
