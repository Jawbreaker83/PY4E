#The len() functiontakes a list as a parameter and returns the number of elements in the list; actually
#len() tells us the number of elements of any set or sequence, i.e. a string;

greet = 'Hello Bob'
print(len(greet))

x = [1,2,'joe',99]
print(len(x))